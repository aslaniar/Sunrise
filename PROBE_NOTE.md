p2-211 staging worktree of the fork repo (nested repo RE_build/Sunrise-fork-inventory).
Carries HEAD 3578d98 plus the 13 uncommitted files that are the currently DEPLOYED
server state, so a build from here is "deployed source + this boot's changes" and
nothing else.
